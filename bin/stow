#!/bin/bash

/usr/local/bin/stow \
    -t $1/ \
    -d $( builtin cd $(dirname $0)/../../ ; pwd -P ) \
    --ignore='.git*' \
    --ignore='.gitconfig' \
    --ignore='LICENSE' \
    --ignore='README.md' \
    --ignore='TODO' \
    --ignore='bin' \
    osxdotfiles

